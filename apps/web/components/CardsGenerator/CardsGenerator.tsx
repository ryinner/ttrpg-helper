export default function CardsGenerator ({ settings }: Props): React.ReactNode {
  return <div>
    
  </div>
}

interface Props {
  settings: Settings
}

export interface Settings {
  width: number;
  height: number;
  gap: number;
}